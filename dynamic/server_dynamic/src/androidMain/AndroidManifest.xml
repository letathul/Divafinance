<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_server_dynamic">
        <dist:delivery>
            <dist:on-demand />
        </dist:delivery>
        <dist:fusing dist:include="true" />
    </dist:module>

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- A local web server matches none of the predefined foreground service types, so
         it declares specialUse with a subtype, as required from API 34. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application>
        <activity
            android:name="com.divafinance.dynamic.server.ServerDynamicActivity"
            android:exported="false" />
        <service
            android:name="com.divafinance.dynamic.server.ServerForegroundService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="local_web_server" />
        </service>
    </application>
</manifest>
