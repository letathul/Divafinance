selectAll:
SELECT * FROM Receipt ORDER BY created_at DESC;

selectById:
SELECT * FROM Receipt WHERE id = ?;

selectByTransactionId:
SELECT * FROM Receipt WHERE transaction_id = ?;

selectByStatus:
SELECT * FROM Receipt WHERE status = ? ORDER BY created_at DESC;

insert:
INSERT INTO Receipt(id, transaction_id, image_path, ocr_text, merchant_name, total_amount, date, status, created_at, subtotal_amount, tax_amount, tip_amount, currency, page_paths)
VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?);

update:
UPDATE Receipt SET transaction_id = ?, image_path = ?, ocr_text = ?, merchant_name = ?, total_amount = ?, date = ?, status = ?, subtotal_amount = ?, tax_amount = ?, tip_amount = ?, currency = ?, page_paths = ?
WHERE id = ?;

delete:
DELETE FROM Receipt WHERE id = ?;
